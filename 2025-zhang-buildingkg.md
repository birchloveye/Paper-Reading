# *Using knowledge graph embeddings and meta-path transformers for geographic context-aware predictions of building functions*

# 全实验流程总结

## 一句话总结

论文将建筑、POI、街景采样点和TAZ组织成异构建筑知识图谱（BuildingKG），再把实体属性与图结构嵌入结合起来，通过9条人工定义的元路径和MP-GTNN预测建筑的主要功能类型。

## 1、研究问题：为什么不能只看建筑本身

传统建筑功能预测通常使用建筑形态、遥感、街景或POI特征，但容易把每栋建筑孤立处理。已有图模型也往往主要建模建筑—建筑关系，不能充分表达建筑与POI、街区和街景之间的空间、语义关系。

论文希望回答：

> 能否把建筑及其周围的多种地理实体和关系统一组织起来，再利用建筑自身特征与地理上下文预测建筑功能？

## 2、研究区域与数据

研究区域为杭州环城高速以内约780 km²的区域，主要涉及上城区、西湖区、拱墅区和滨江区。

- 106,851个建筑；
- 1,895个TAZ（Transportation Analysis Zones）；
- 285,552个POI；
- 53,136个SVSP（Street-View Sampling Points）；
- 2,135,599个三元组；
- 8类实体关系。

数据来源包括高德建筑数据、OSM道路、Amap POI、Baidu街景和Sentinel-2遥感影像。建筑功能标签先通过AOI叠加自动生成，再由具有专业知识的志愿者利用遥感、街景和POI进行核验。

## 3、构建BuildingKG

### 3.1 提取实体

- Building：预测对象，包含建筑自身形态信息；
- POI：表示建筑内部或附近的服务和活动地点；
- SVSP：与建筑一一对应的街景采样点；
- TAZ：根据道路中心线分割出的交通分析区，用于对齐不同来源的数据和表达街区环境。

### 3.2 建立空间和语义关系

图谱中的事实写成三元组：

```text
（头实体，关系，尾实体）
```

主要关系包括：

- Building—Building：`adjacentTo`，通过约束Delaunay三角网确定相邻建筑；
- TAZ—TAZ：通过TAZ质心的Delaunay三角网确定相邻街区；
- Building—TAZ：`locateWithin`，表示建筑位于某TAZ内；
- POI—Building：`locateInBuffer`，表示POI位于建筑约10米缓冲区内；
- TAZ—Building：`contain`，是`locateWithin`的逆关系；
- POI—POI Category：`hasCategory`，表示POI所属类别；
- SVSP—Building：`hasSviOf`，表示街景采样点对应哪个建筑；
- SVSP—TAZ：表示SVSP位于哪个TAZ内。

## 4、提取节点属性

不同实体使用不同的属性特征：

- Building：面积、周长、高度、方向、平均半径、伸长率、圆形度、凸度、矩形度和密度等形态特征；
- TAZ：区域内建筑的平均面积、边界、高度、密度等形态统计量，以及NDVI、NDBI、NDWI等遥感光谱特征；
- POI：重新分类后使用one-hot类别特征；
- SVSP：使用在ADE20K上训练的PSPNet进行街景语义分割，识别150类视觉元素，并用各类元素的面积比例形成150维视觉向量。

注意：这些属性不是直接拼成一条原始数据表，而是挂在对应的图节点上。

## 5、训练GIE知识图谱嵌入

BuildingKG中存在链、层级和环等复合结构。论文使用GIE（Geometry Interaction Knowledge Graph Embeddings）在三种几何空间中学习实体和关系：

- 欧氏空间；
- 双曲空间；
- 超球空间。

GIE先为实体和关系初始化向量，再通过几何交互机制融合三种空间，计算三元组的可信度：真实三元组应得高分，负采样产生的错误三元组应得低分。模型使用均匀负采样和交叉熵损失进行训练。

训练完成后，每个实体得到一个低维结构嵌入。这个向量不是建筑的面积或街景颜色，而是表示：

- 实体在知识图谱中的结构位置；
- 它和哪些实体相连；
- 这些关系构成了怎样的空间和语义结构。

之后，将每个实体的属性特征与其GIE结构嵌入拼接，作为图节点特征。

## 6、构造9条元路径

元路径是连接异构图中不同实体的关系序列。论文使用9条人工定义的元路径：

- B：建筑自身的形态特征；
- BB：相邻建筑的形态特征；
- BP：该建筑关联POI的类别特征；
- BT：建筑所在TAZ的光谱和形态特征；
- BS：该建筑对应的街景视觉特征；
- BBP：相邻建筑关联POI的类别特征；
- BTT：相邻TAZ的光谱特征；
- BTS：建筑所在TAZ中街景采样点的整体视觉特征；
- BTBP：建筑所在TAZ中建筑关联POI的整体类别特征。

这些路径分别从建筑自身、邻近建筑、POI、街区、街景和更远的空间上下文描述同一个建筑。

## 7、MP-GTNN如何进行建筑功能预测

MP-GTNN包含四个部分：

### 7.1 元路径邻居聚合

对于每条元路径，模型找到目标建筑能够通过该路径连接到的实体，并对这些实体特征做一次均值聚合：

```text
某建筑沿某条元路径连接到的实体特征
                         ↓
                       Mean
                         ↓
                 该元路径的特征表示
```

这一步是确定性的均值聚合，没有可学习参数。

### 7.2 特征投影

不同元路径上的实体特征维度不一样，因此分别通过MLP投影到统一维度。

### 7.3 Transformer融合

将9条元路径得到的特征表示看成一个特征序列，输入Transformer编码器。Transformer使用多头自注意力（MHSA）和前馈网络（FFN），学习不同元路径特征之间的相互作用，再将各路径的输出拼接起来。

这里要特别注意：MP-GTNN不是先让建筑数据、POI数据和TAZ数据各自独立预测一次，再把几个预测结果相加；它是在特征层面联合学习这些证据如何组合。

### 7.4 输出建筑功能

经过最终MLP和Softmax后，输出9类建筑功能的概率，概率最高的类别作为预测结果：

- Residential；
- Shopping & Catering；
- Business Office；
- Communal Facilities；
- Educational；
- Medical；
- Government；
- Hotel；
- Factory & Industrial Park。

## 8、训练、对比和结果

建筑样本按6:2:2划分训练集、验证集和测试集。论文将训练样本比例设置为10%到60%进行比较，并使用OA、Macro-F1和Kappa评价建筑功能预测。

对比模型包括RF、SVM、XGBoost、FNN、GCN、GraphSAGE、R-GCN和Simple-HGN。论文还构造了去掉BuildingKG嵌入的MP-GTNN变体，用来检验GIE结构知识的作用。

主要结果：

- 杭州完整实验中，MP-GTNN在60%训练比例下OA为93.05%；
- 去掉GIE结构嵌入后，性能下降；
- 在深圳福田的跨地区实验中，使用已训练MP-GTNN得到84.28%的OA，而去掉GIE嵌入后为78.19%；
- SHAP分析显示，BP、BBP、BT、BB和B是影响较大的特征组。

这些结果支持这样的解释：POI、TAZ、街景和建筑形态都能提供线索，但它们可能互相矛盾；GIE提供的实体关系结构有助于判断这些线索应该如何理解。

## 9、跨地区预测是怎样进行的

论文在深圳福田进行了泛化实验。流程是：

```text
杭州数据和标签
        ↓
训练GIE与MP-GTNN
        ↓
福田多源数据
        ↓
重新构建福田BuildingKG并获得实体嵌入
        ↓
使用训练好的MP-GTNN预测福田建筑功能
```

论文明确说将训练好的MP-GTNN应用于福田，但没有明确说明福田阶段的GIE参数是完全冻结后直接使用，还是在福田图谱上重新获得嵌入。因此，不能简单说整条模型都完全冻结；比较确定的是，分类用的MP-GTNN被直接迁移到新地区，没有用福田标签重新训练。

## 10、这篇论文的本质

MP-GTNN本质上是一个监督学习的异构图神经网络。它学习的是：

> 给定建筑自身特征、周边实体特征、街区特征、街景特征和实体关系结构，如何映射到建筑功能类别。

因此，多源数据不是分别产生多个独立结论，而是共同构成证据。模型结构和计算流程是清楚的，但训练后的具体参数不具有直接的人类语义，属于结构透明、参数解释较困难的半黑盒模型。

## 11、主要局限与阅读评价

- 元路径是人工定义的，在更复杂城市环境中可能遗漏有用关系；
- 只使用了有限类型的城市实体，泛化能力仍需更多地区和数据验证；
- 随机空间划分可能让空间相近、分布相似的建筑同时出现在训练集和测试集，从而高估泛化性能；
- GIE、实体属性和元路径特征之间是否真正互补，论文的消融实验还没有完全拆开验证；
- POI过度集中或过于稀疏时可能误导模型；
- 标签主要表示建筑的单一主功能，难以表达现实中的混合功能建筑。

总体上，这是一篇数据规模较大、方法流程完整的应用型论文。它的核心价值是把建筑功能预测从“单栋建筑分类”扩展成“结合城市地理上下文的异构图分类”。
